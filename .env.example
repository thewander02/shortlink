# PostgreSQL connection string
# For local development with docker-compose: postgresql://shortlink:shortlink@localhost:5433/shortlink?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/shortlink?schema=public"

# (Optional - for caching and rate limiting)
KV_REST_API_URL="https://your-redis-instance.upstash.io"
KV_REST_API_TOKEN="your-redis-token"

# Application
# Base URL for shortened links (e.g., https://l.example.com)
PUBLIC_LINK_URL="https://l.example.com"

# Admin
# Secret key for admin panel access (minimum 32 characters recommended)
ADMIN_SECRET_KEY="your-super-secret-admin-key-minimum-32-characters-long"

# Cron Jobs (Optional)
# Secret token for authenticating cron job requests
CRON_SECRET_TOKEN="your-cron-secret-token"
